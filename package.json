{
  "name": "onix",
  "description": "CSS for arbok.ai",
  "version": "0.0.1",
  "scripts": {
    "build": "sass --load-path=node_modules onix.scss ./dist/onix.css --style compressed",
    "start": "npm run build -- --watch"
  },
  "dependencies": {
    "bulma": "^1.0.2",
    "sass": "^1.77.8"
  }
}
